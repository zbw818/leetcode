class Solution {
public:
    void reverseString(vector<char>& s) {
        char tem;
        int len=s.size();
        for(int i=0;i<len/2;i++){
            tem=s[i];
            s[i]=s[len-1-i];
            s[len-1-i]=tem;
        }
    }
};
